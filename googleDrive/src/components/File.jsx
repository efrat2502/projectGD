import React from "react";

const File = () => {
  return (
    <div>
      {" "}
      {/* <button>{f.name}</button>
  {f.isDir ? <span>📁</span> : <span>📝</span>}
  <br /> */}
    </div>
  );
};

export default File;
